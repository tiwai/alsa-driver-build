--- ../alsa-kernel/firewire/scs1x.c	2013-07-15 12:14:30.252860918 +0200
+++ scs1x.c	2013-07-15 17:02:16.852565017 +0200
@@ -1,3 +1,4 @@
+#include "adriver.h"
 /*
  * Stanton Control System 1 MIDI driver
  *
@@ -496,6 +497,19 @@
 };
 MODULE_DEVICE_TABLE(ieee1394, scs_id_table);
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(3, 11, 0)
+static int __scs_probe(struct device *dev)
+{
+	return scs_probe(fw_unit(dev), NULL);
+}
+
+static int __scs_remove(struct device *dev)
+{
+	scs_remove(fw_unit(dev));
+	return 0;
+}
+#endif
+
 MODULE_DESCRIPTION("SCS.1x MIDI driver");
 MODULE_AUTHOR("Clemens Ladisch <clemens@ladisch.de>");
 MODULE_LICENSE("GPL v2");
@@ -505,10 +519,16 @@
 		.owner  = THIS_MODULE,
 		.name   = KBUILD_MODNAME,
 		.bus    = &fw_bus_type,
+#if LINUX_VERSION_CODE < KERNEL_VERSION(3, 11, 0)
+		.probe    = __scs_probe,
+		.remove   = __scs_remove,
+#endif
 	},
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(3, 11, 0)
 	.probe    = scs_probe,
-	.update   = scs_update,
 	.remove   = scs_remove,
+#endif
+	.update   = scs_update,
 	.id_table = scs_id_table,
 };
 
